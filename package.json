{
  "name": "lite-notepad",
  "version": "0.0.1",
  "description": "A tiny notepad",
  "main": "main.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=develop electron .",
    "start": "cross-env NODE_ENV=production electron .",
    "package": "cross-env NODE_ENV=production electron-packager ./ MyApp --all --out ~/Desktop/MyApp --version 1.4.1 --overwrite --icon=./app/img/icon.icns"
  },
  "devDependencies": {
    "cross-env": "^3.0.0",
    "electron": "^1.3.4",
    "electron-packager": "^8.1.0"
  },
  "dependencies": {}
}